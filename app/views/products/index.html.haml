
- @products.each do |product|
  商品名
  %p= product.product_name


- @products.each do |product|
  = link_to product_path(product) do
    - product.images.each do |image|
      商品画像
      = image_tag "#{image.src}", class: "img"

- @products.each do |product|
  商品説明
  %p= product.product_explanation

- @products.each do |product|
  ブランド(任意)
  %p= product.brand

- @products.each do |product|
  商品の状態
  %p= product.product_status_id

- @products.each do |product|
  発送方法
  %p= product.shipping_method_id

- @products.each do |product|
  配送料の負担
  %p= product.shipping_charge_id

- @products.each do |product|
  発送地域
  %p= product.prefecture_id

- @products.each do |product|
  配送までの日数
  %p= product.days_until_shipping_id

- @products.each do |product|
  価格
  %p= product.price

.Products
  .Products__main
    .Products__main__content
      .Products__title
        .Products__title__box
          Tシャツ/カットソー(半袖/半袖なし)の商品一覧
      .Products__list
        .Products__listBox
          - @products.each do |product|
            .ProductBox
              .ProductBox__image
                -# = product.image
                -# = image_tag image.image.url, class: "Product__image"
                .ProductBox__body
                  .ProductName
                    = product.product_name
                  .Price
                    = product.price

        .ProductBox__image__each
          %img{src: "image/image.jpg", alt: "Sold"}
          -# - @images.each do |image|
          -# = image_tag image.image.url

